[package]
name = "mira-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = { workspace = true }
mira-core = { path = "../../core" }
sqlx = { version = "0.8", features = [ "sqlite", "runtime-tokio" ] }
